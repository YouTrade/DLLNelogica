# Suíte determinística de estados

Execute a partir da raiz do repositório:

    dotnet run --project tests/DLLNelogica.StateTests.csproj --configuration Release -p:Platform=x64

A suíte não carrega a ProfitDLL, não acessa o mercado e não utiliza rede.
